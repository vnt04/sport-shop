const routes = {
    home: '/',
    category: '/category',
    orders: '/orders',
    store: '/store',
    impWarehouse: '/impWarehouse',
    expWarehouse: '/expWarehouse',
};

export default routes;
